# Core ETL Dependencies
pandas>=2.1.0
sqlalchemy>=1.4.0
python-dotenv>=1.0.0

# Testing Dependencies
pytest>=7.4.0
flake8>=6.0.0

# Database Drivers (for local development only)
psycopg2-binary>=2.9.0

# Note: Apache Airflow is handled by Docker containers
# For local development, we only need the ETL dependencies
